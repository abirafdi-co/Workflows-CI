name: ml_telco_churn_env

channels:
  - conda-forge

dependencies:
  - python=3.12.7
  - pip
  - pip:
      - mlflow=2.10.0
      - scikit-learn
      - pandas
      - numpy
      - dagshub
